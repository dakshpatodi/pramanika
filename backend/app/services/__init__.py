"""
Service layer package.

Intentionally empty in Phase 1. This is where business logic will live
in later phases (e.g. `services/product_service.py`, `services/cart_service.py`),
kept separate from API route handlers so routes stay thin.
"""
