"""
Pydantic schemas package (request/response DTOs).

Intentionally empty in Phase 1 - no API resources exist yet beyond the
health check, which returns a plain dict. Phase 2 will add one module
per resource here, e.g. `schemas/product.py`.
"""
