"""
API routers package.

Intentionally empty in Phase 1. The health check lives directly on the
FastAPI app in `main.py` for now. From Phase 2 onwards, versioned routers
will be added here (e.g. `api/v1/products.py`) and included in `main.py`
via `app.include_router(...)`.
"""
